
```
Daily usage:

    Start docker server
        ? docker-sync clean
        ? docker-sync start
        ? ./vessel start

    SSH into server
        ? ./vessel ssh app

    Stop server
        ? ./vessel stop
        ? docker-sync stop

To view site:
    Open your browser to http://localhost
```
